{
  "name": "perfpulse",
  "version": "0.1.21",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun run scripts/build.ts",
    "check": "bun run lint && bun run typecheck && bun test && bun run build",
    "ci": "bun install --frozen-lockfile",
    "kind:smoke": "bash scripts/kind-smoke.sh",
    "k6:dry-run": "bun run build && PERF_PULSE_CLIENT_MODE=noop TESTID=local-noop k6 run dist/perfpulse.js",
    "k6:dry-run:html": "bun run build && mkdir -p artifacts/local && PERF_PULSE_CLIENT_MODE=noop TESTID=local-noop NOOP_SLEEP_SECONDS=10 K6_WEB_DASHBOARD=true K6_WEB_DASHBOARD_PERIOD=1s K6_WEB_DASHBOARD_EXPORT=artifacts/local/perfpulse-noop.html k6 run dist/perfpulse.js",
    "lint": "biome check .",
    "skaha-auth-cleanup": "bun run scripts/skaha-auth.ts --cleanup",
    "skaha-auth-setup": "bun run scripts/skaha-auth.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.14",
    "@types/bun": "^1.3.13",
    "@types/k6": "^1.7.0",
    "typescript": "^6.0.3"
  }
}
